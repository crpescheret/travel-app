<div class="ui container">
  <% if current_user %>

  <div ng-app="app">
    <div ng-controller="tripsCtrl">
      
      <h1 class="ui header">My Trips</h1>

      <% @trips.each do |trip| %>
        <a href='trips/<%= trip.id%>'><%= trip.title %></p>
      <% end %>
      <br>
      <h3><a href = '/trips/new'>Create New Trip</a></h3>
      <h3><a href='/groups/new'>Add to Existing Trip</a></h3>

      <% end %>
    </div>
  </div>
</div>